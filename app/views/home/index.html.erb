<!-- Hero Section -->
<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <div class="hero-badge">
          <span class="badge-text">Connect • Match • Meet</span>
        </div>
        <h1 class="hero-title">Meet people at events you love</h1>
        <p class="hero-subtitle">Swipe through fellow attendees, make connections, and turn events into meaningful relationships. Join thousands of people already connecting.</p>
        
        <div class="hero-stats">
          <div class="stat-item">
            <div class="stat-number">10K+</div>
            <div class="stat-label">Active Users</div>
          </div>
          <div class="stat-item">
            <div class="stat-number">500+</div>
            <div class="stat-label">Events Monthly</div>
          </div>
          <div class="stat-item">
            <div class="stat-number">95%</div>
            <div class="stat-label">Match Success</div>
          </div>
        </div>
        
        <div class="hero-actions">
          <%= link_to signup_path, class: "btn btn-primary btn-lg hero-cta" do %>
            Get Started Free
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          <% end %>
          <%= link_to login_path, class: "btn btn-secondary btn-lg" do %>
            Sign In
          <% end %>
        </div>
        
        <div class="hero-trust">
          <div class="trust-avatars">
            <div class="trust-avatar">M</div>
            <div class="trust-avatar">A</div>
            <div class="trust-avatar">S</div>
            <div class="trust-avatar">+</div>
          </div>
          <p class="trust-text">Join 10,000+ people making connections</p>
        </div>
      </div>
      
      <div class="hero-visual">
        <div class="phone-mockup">
          <div class="phone-screen">
            <div class="swipe-demo">
              <div class="demo-card active">
                <div class="demo-avatar">A</div>
                <div class="demo-info">
                  <h4>Alex Chen</h4>
                  <p>Tech Conference 2024</p>
                  <div class="demo-interests">
                    <span class="interest-tag">AI</span>
                    <span class="interest-tag">Startups</span>
                  </div>
                </div>
              </div>
              <div class="demo-card">
                <div class="demo-avatar">S</div>
                <div class="demo-info">
                  <h4>Sarah Kim</h4>
                  <p>Music Festival</p>
                  <div class="demo-interests">
                    <span class="interest-tag">Music</span>
                    <span class="interest-tag">Art</span>
                  </div>
                </div>
              </div>
              <div class="demo-actions">
                <button class="demo-btn demo-pass">✕</button>
                <button class="demo-btn demo-like">♡</button>
              </div>
            </div>
          </div>
          <div class="phone-glow"></div>
        </div>
        
        <!-- Floating elements -->
        <div class="floating-elements">
          <div class="floating-icon icon-1">💬</div>
          <div class="floating-icon icon-2">🎉</div>
          <div class="floating-icon icon-3">✨</div>
          <div class="floating-icon icon-4">❤️</div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Background pattern -->
  <div class="hero-pattern">
    <div class="pattern-dot"></div>
    <div class="pattern-dot"></div>
    <div class="pattern-dot"></div>
    <div class="pattern-dot"></div>
    <div class="pattern-dot"></div>
  </div>
</section>

<!-- Social Proof Section -->
<section class="social-proof">
  <div class="container">
    <div class="social-content">
      <p class="social-title">Trusted by people at events worldwide</p>
      <div class="testimonials-grid">
        <div class="testimonial">
          <div class="testimonial-content">
            <p>"I met my best friend at a conference through heitsu. The app made networking so much easier!"</p>
          </div>
          <div class="testimonial-author">
            <div class="author-avatar">M</div>
            <div class="author-info">
              <div class="author-name">Maria Santos</div>
              <div class="author-title">Product Designer</div>
            </div>
          </div>
        </div>
        
        <div class="testimonial">
          <div class="testimonial-content">
            <p>"Found my co-founder at a startup event. heitsu connects you with the right people at the right time."</p>
          </div>
          <div class="testimonial-author">
            <div class="author-avatar">J</div>
            <div class="author-info">
              <div class="author-name">James Wilson</div>
              <div class="author-title">Startup Founder</div>
            </div>
          </div>
        </div>
        
        <div class="testimonial">
          <div class="testimonial-content">
            <p>"Amazing for making friends who share your interests. I've connected with so many like-minded people."</p>
          </div>
          <div class="testimonial-author">
            <div class="author-avatar">L</div>
            <div class="author-info">
              <div class="author-name">Lisa Park</div>
              <div class="author-title">Marketing Manager</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- How It Works -->
<section class="how-it-works">
  <div class="container">
    <div class="text-center mb-6">
      <h2>How it works</h2>
      <p class="text-grey">Three simple steps to start connecting</p>
    </div>
    
    <div class="grid grid-3">
      <div class="step-card">
        <div class="step-number">1</div>
        <h3>Find Events</h3>
        <p>Browse events that match your interests and register to attend.</p>
      </div>
      
      <div class="step-card">
        <div class="step-number">2</div>
        <h3>Swipe & Match</h3>
        <p>Swipe through other attendees at events. Match with people you'd like to meet.</p>
      </div>
      
      <div class="step-card">
        <div class="step-number">3</div>
        <h3>Connect</h3>
        <p>Connect directly with matched users. Start conversations and plan to meet up.</p>
      </div>
    </div>
  </div>
</section>

<!-- Features -->
<section class="features">
  <div class="container">
    <div class="text-center mb-6">
      <h2>Why choose heitsu?</h2>
      <p class="text-grey">Everything you need to make meaningful connections</p>
    </div>
    
    <div class="grid grid-2">
      <div class="feature-card">
        <div class="feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
          </svg>
        </div>
        <h3>Smart Matching</h3>
        <p>Our algorithm connects you with people who share your interests and event preferences.</p>
      </div>
      
      <div class="feature-card">
        <div class="feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
            <circle cx="9" cy="7" r="4"></circle>
            <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
            <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
          </svg>
        </div>
        <h3>Event Connections</h3>
        <p>Connect with multiple people at the same event. Build your social network efficiently.</p>
      </div>
      
      <div class="feature-card">
        <div class="feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
          </svg>
        </div>
        <h3>Real-time Chat</h3>
        <p>Start conversations before the event and continue them in person.</p>
      </div>
      
      <div class="feature-card">
        <div class="feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
            <line x1="16" y1="2" x2="16" y2="6"></line>
            <line x1="8" y1="2" x2="8" y2="6"></line>
            <line x1="3" y1="10" x2="21" y2="10"></line>
          </svg>
        </div>
        <h3>Event Discovery</h3>
        <p>Discover new events and expand your social circle beyond your usual activities.</p>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="cta">
  <div class="container">
    <div class="cta-content">
      <h2>Ready to start connecting?</h2>
      <p>Join thousands of people making meaningful connections at events.</p>
      <%= link_to "Sign up now", signup_path, class: "btn btn-primary btn-lg" %>
    </div>
  </div>
</section>

<style>
  /* Hero Section */
  .hero {
    background: linear-gradient(135deg, var(--color-grey-50) 0%, var(--color-white) 100%);
    padding: 5rem 0;
    min-height: 90vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: center;
    position: relative;
    z-index: 2;
  }

  .hero-badge {
    background: linear-gradient(45deg, var(--color-primary), var(--color-primary-dark));
    color: var(--color-black);
    padding: 0.5rem 1rem;
    border-radius: 50px;
    font-size: var(--font-size-sm);
    font-weight: 600;
    display: inline-block;
    margin-bottom: 1.5rem;
    animation: pulse 2s infinite;
  }

  .badge-text {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }

  .hero-title {
    font-size: var(--font-size-4xl);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--color-black);
    background: linear-gradient(135deg, var(--color-black), var(--color-grey-700));
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .hero-subtitle {
    font-size: var(--font-size-xl);
    color: var(--color-grey-600);
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .hero-stats {
    display: flex;
    gap: 2rem;
    margin-bottom: 2.5rem;
    padding: 1.5rem 0;
  }

  .stat-item {
    text-align: center;
  }

  .stat-number {
    font-size: var(--font-size-2xl);
    font-weight: 800;
    color: var(--color-primary-dark);
    margin-bottom: 0.25rem;
  }

  .stat-label {
    font-size: var(--font-size-sm);
    color: var(--color-grey-600);
    font-weight: 500;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-bottom: 2.5rem;
  }

  .hero-cta {
    position: relative;
    overflow: hidden;
  }

  .hero-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    transition: left 0.6s;
  }

  .hero-cta:hover::before {
    left: 100%;
  }

  .hero-trust {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .trust-avatars {
    display: flex;
    margin-right: 0.5rem;
  }

  .trust-avatar {
    width: 2.5rem;
    height: 2.5rem;
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark));
    border-radius: 50%;
    border: 2px solid var(--color-white);
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    font-size: var(--font-size-sm);
    color: var(--color-black);
    margin-left: -0.5rem;
    box-shadow: var(--shadow-sm);
  }

  .trust-avatar:first-child {
    margin-left: 0;
  }

  .trust-text {
    font-size: var(--font-size-sm);
    color: var(--color-grey-600);
    margin: 0;
    font-weight: 500;
  }

  .hero-pattern {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    opacity: 0.05;
    pointer-events: none;
  }

  .pattern-dot {
    position: absolute;
    width: 4px;
    height: 4px;
    background: var(--color-primary);
    border-radius: 50%;
  }

  .pattern-dot:nth-child(1) { top: 20%; left: 10%; animation: float 3s infinite; }
  .pattern-dot:nth-child(2) { top: 40%; right: 20%; animation: float 4s infinite 1s; }
  .pattern-dot:nth-child(3) { bottom: 30%; left: 15%; animation: float 5s infinite 2s; }
  .pattern-dot:nth-child(4) { top: 60%; left: 50%; animation: float 3.5s infinite 0.5s; }
  .pattern-dot:nth-child(5) { bottom: 20%; right: 10%; animation: float 4.5s infinite 1.5s; }

  /* Phone Mockup */
  .hero-visual {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .phone-mockup {
    width: 300px;
    height: 600px;
    background: linear-gradient(145deg, var(--color-black), var(--color-black-light));
    border-radius: 2.5rem;
    padding: 1rem;
    margin: 0 auto;
    position: relative;
    box-shadow: var(--shadow-xl);
    animation: phoneFloat 4s ease-in-out infinite;
  }

  @keyframes phoneFloat {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-10px) rotate(1deg); }
  }

  .phone-screen {
    width: 100%;
    height: 100%;
    background: var(--color-white);
    border-radius: 2rem;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .phone-glow {
    position: absolute;
    top: -20px;
    left: -20px;
    right: -20px;
    bottom: -20px;
    background: linear-gradient(45deg, var(--color-primary), var(--color-primary-dark));
    border-radius: 3rem;
    opacity: 0.1;
    animation: glow 3s ease-in-out infinite alternate;
    z-index: -1;
  }

  @keyframes glow {
    0% { opacity: 0.1; transform: scale(1); }
    100% { opacity: 0.2; transform: scale(1.05); }
  }

  .floating-elements {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
  }

  .floating-icon {
    position: absolute;
    font-size: 1.5rem;
    background: var(--color-white);
    border-radius: 50%;
    width: 3rem;
    height: 3rem;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-lg);
    animation: floatIcon 6s ease-in-out infinite;
  }

  .icon-1 {
    top: 20%;
    left: -10%;
    animation-delay: 0s;
  }

  .icon-2 {
    top: 50%;
    right: -15%;
    animation-delay: 1.5s;
  }

  .icon-3 {
    bottom: 30%;
    left: -5%;
    animation-delay: 3s;
  }

  .icon-4 {
    top: 10%;
    right: -10%;
    animation-delay: 4.5s;
  }

  @keyframes floatIcon {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    33% { transform: translateY(-15px) rotate(5deg); }
    66% { transform: translateY(5px) rotate(-5deg); }
  }

  .swipe-demo {
    position: relative;
    width: 200px;
    height: 300px;
  }

  .demo-card {
    position: absolute;
    top: 20px;
    left: 20px;
    right: 20px;
    background: var(--color-white);
    border: 2px solid var(--color-primary);
    border-radius: var(--radius-xl);
    padding: 2rem 1.5rem 1rem;
    text-align: center;
    box-shadow: var(--shadow-lg);
    transition: var(--transition);
  }

  .demo-card.active {
    z-index: 2;
    animation: cardFloat 3s ease-in-out infinite;
  }

  .demo-card:not(.active) {
    transform: scale(0.95) translateY(10px);
    opacity: 0.7;
  }

  .demo-avatar {
    width: 4rem;
    height: 4rem;
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: var(--font-size-2xl);
    color: var(--color-black);
    margin: 0 auto 1rem;
  }

  .demo-info h4 {
    margin-bottom: 0.5rem;
    color: var(--color-black);
    font-size: var(--font-size-lg);
  }

  .demo-info p {
    color: var(--color-grey-600);
    font-size: var(--font-size-sm);
    margin-bottom: 1rem;
  }

  .demo-interests {
    display: flex;
    gap: 0.5rem;
    justify-content: center;
    margin-bottom: 1rem;
  }

  .interest-tag {
    background: var(--color-primary-light);
    color: var(--color-black);
    padding: 0.25rem 0.75rem;
    border-radius: 50px;
    font-size: var(--font-size-xs);
    font-weight: 500;
  }

  .demo-actions {
    position: absolute;
    bottom: -60px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 1rem;
    z-index: 3;
  }

  .demo-btn {
    width: 3rem;
    height: 3rem;
    border: none;
    border-radius: 50%;
    font-size: 1.25rem;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-md);
  }

  .demo-pass {
    background: var(--color-white);
    color: #ef4444;
    border: 2px solid #ef4444;
  }

  .demo-pass:hover {
    background: #ef4444;
    color: var(--color-white);
    transform: scale(1.1);
  }

  .demo-like {
    background: var(--color-primary);
    color: var(--color-black);
    border: 2px solid var(--color-primary);
  }

  .demo-like:hover {
    background: var(--color-primary-dark);
    transform: scale(1.1);
  }

  @keyframes cardFloat {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-10px) rotate(2deg); }
  }

  /* Social Proof Section */
  .social-proof {
    padding: 4rem 0;
    background: var(--color-white);
    border-bottom: 1px solid var(--color-grey-200);
  }

  .social-content {
    text-align: center;
  }

  .social-title {
    font-size: var(--font-size-lg);
    color: var(--color-grey-600);
    margin-bottom: 3rem;
    font-weight: 500;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .testimonial {
    background: var(--color-grey-50);
    border-radius: var(--radius-xl);
    padding: 2rem;
    border: 1px solid var(--color-grey-200);
    transition: var(--transition);
    position: relative;
  }

  .testimonial:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
    border-color: var(--color-primary);
  }

  .testimonial-content {
    margin-bottom: 1.5rem;
  }

  .testimonial-content p {
    font-style: italic;
    color: var(--color-grey-700);
    font-size: var(--font-size-base);
    line-height: 1.6;
    margin: 0;
  }

  .testimonial-author {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .author-avatar {
    width: 3rem;
    height: 3rem;
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: var(--font-size-lg);
    color: var(--color-black);
    flex-shrink: 0;
  }

  .author-info {
    text-align: left;
  }

  .author-name {
    font-weight: 600;
    color: var(--color-black);
    margin-bottom: 0.25rem;
  }

  .author-title {
    font-size: var(--font-size-sm);
    color: var(--color-grey-600);
  }

  /* How It Works */
  .how-it-works {
    padding: 6rem 0;
    background: var(--color-white);
  }

  .step-card {
    text-align: center;
    padding: 2rem;
  }

  .step-number {
    width: 4rem;
    height: 4rem;
    background: var(--color-primary);
    color: var(--color-black);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--font-size-2xl);
    font-weight: 800;
    margin: 0 auto 1.5rem;
  }

  .step-card h3 {
    margin-bottom: 1rem;
    color: var(--color-black);
  }

  .step-card p {
    color: var(--color-grey-600);
    max-width: 250px;
    margin: 0 auto;
  }

  /* Features */
  .features {
    padding: 6rem 0;
    background: var(--color-grey-50);
  }

  .feature-card {
    background: var(--color-white);
    padding: 2.5rem;
    border-radius: var(--radius-xl);
    border: 1px solid var(--color-grey-200);
    transition: var(--transition);
    text-align: center;
  }

  .feature-card:hover {
    border-color: var(--color-primary);
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
  }

  .feature-icon {
    width: 3rem;
    height: 3rem;
    color: var(--color-primary);
    margin: 0 auto 1.5rem;
  }

  .feature-icon svg {
    width: 100%;
    height: 100%;
    stroke-width: 2;
  }

  .feature-card h3 {
    margin-bottom: 1rem;
    color: var(--color-black);
  }

  .feature-card p {
    color: var(--color-grey-600);
  }

  /* CTA */
  .cta {
    padding: 6rem 0;
    background: linear-gradient(135deg, var(--color-black) 0%, var(--color-black-light) 100%);
    color: var(--color-white);
  }

  .cta-content {
    text-align: center;
    max-width: 600px;
    margin: 0 auto;
  }

  .cta h2 {
    color: var(--color-white);
    margin-bottom: 1rem;
  }

  .cta p {
    color: var(--color-grey-300);
    font-size: var(--font-size-lg);
    margin-bottom: 2.5rem;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero {
      padding: 4rem 0;
      min-height: 70vh;
    }

    .hero-content {
      grid-template-columns: 1fr;
      gap: 3rem;
      text-align: center;
    }

    .hero-title {
      font-size: var(--font-size-3xl);
    }

    .hero-subtitle {
      font-size: var(--font-size-lg);
    }

    .hero-stats {
      justify-content: center;
      gap: 1.5rem;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-trust {
      justify-content: center;
    }

    .phone-mockup {
      width: 260px;
      height: 520px;
    }

    .floating-elements {
      display: none; /* Hide floating icons on tablet and mobile */
    }

    .social-proof {
      padding: 3rem 0;
    }

    .testimonials-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .how-it-works, .features, .cta {
      padding: 4rem 0;
    }

    .grid-3 {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .grid-2 {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding: 3rem 0;
    }

    .hero-title {
      font-size: var(--font-size-2xl);
    }

    .hero-badge {
      font-size: var(--font-size-xs);
      padding: 0.375rem 0.75rem;
    }

    .hero-stats {
      flex-direction: column;
      gap: 1rem;
      padding: 1rem 0;
    }

    .stat-item {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      justify-content: center;
    }

    .stat-number {
      font-size: var(--font-size-xl);
    }

    .trust-avatars {
      scale: 0.9;
    }

    .hero-actions .btn {
      width: 100%;
      flex: 1;
    }

    .hero-actions {
      flex-direction: column;
      gap: 0.75rem;
    }

    .phone-mockup {
      width: 220px;
      height: 440px;
    }

    .demo-card {
      padding: 1.5rem 1rem 0.5rem;
    }

    .demo-actions {
      bottom: -50px;
    }

    .demo-btn {
      width: 2.5rem;
      height: 2.5rem;
      font-size: 1rem;
    }

    .social-proof {
      padding: 2rem 0;
    }

    .testimonial {
      padding: 1.5rem;
    }

    .testimonials-grid {
      gap: 1rem;
    }

    .step-card, .feature-card {
      padding: 1.5rem;
    }

    .hero-pattern {
      display: none; /* Hide pattern on mobile for performance */
    }
  }
</style> 